Se implementará en C++ la versión del algoritmo de Dijkstra de coste $`\Theta((a+n)\log n)`$
que usa un montículo. Para ello se programará un montículo sesgado con la operación adicional
de decrecer-clave.
